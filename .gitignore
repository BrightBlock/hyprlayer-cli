/target
/logs
/tools
/dashboards

# Release asset bundles built by `scripts/build-asset-bundles.sh`.
/dist

# Local Claude Code state — settings, worktrees, etc. Never committed.
.claude/

# Conductor preference texts
docs/conductor-preferences/

# hyprlayer thoughts — symlinks to a separate repo managed by `hyprlayer thoughts sync`.
/thoughts/
